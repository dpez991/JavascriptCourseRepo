// Convertir de numero a cadena a JS
let a = 10, b = 20;
// Si son valores numericos se suman
console.log(a + b);

// Convertir a cadena para que se concatenen
let concatenacion = a.toString() + b.toString();
console.log(concatenacion);